This is a Visual Studio 2010 console application to run the Windows verison of OpenScad from the command line.

This is neccessary because a Windows app cannot be both a console app and a GUI app because the shell makes that descision based on a flag in the EXE before it is run.

Based on code from http://kaioa.com/node/63 with a zero-clause BSD-style license.
